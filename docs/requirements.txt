# Documentation build dependencies (MkDocs + Material).
# Pinned so a Read the Docs rebuild of an old commit renders the same way it
# did when the commit was written.
mkdocs==1.6.1
mkdocs-material==9.7.7
