# ci/MUTATION_COVERAGE — which signoff gates are PROVEN able to fail
#
# A joint work commissioned on behalf of SoC Labs, under Arm Academic Access
# license.  Copyright 2026, SoC Labs (www.soclabs.org)
#
# ---------------------------------------------------------------------------
# WHAT THIS FILE IS, AND WHY IT IS NOT THE TOOLKIT'S LEDGER
#
# ASIC/asic-toolkit/test/MUTATION_COVERAGE declares how many planted faults the
# TOOLKIT's own suite must reject — its Tcl flow procs, its static gates, its
# stage scenarios. That toolkit is a pinned submodule shared with the compute
# chiplet, and its numbers are about code that is identical on both.
#
# This file is the other half, and it is deliberately HERE rather than there:
# ci/signoff.yaml is, in its own words, "THE ONLY PART THAT DIFFERS BETWEEN
# CHIPLETS". A gate matrix for these stages is a fact about THIS design, so
# recording it in the shared submodule would both bump a pinned pointer and make
# one chiplet's coverage look like the other's.
#
# ---------------------------------------------------------------------------
# HOW TO READ IT
#
#   PROVEN     the stage has a `check:` that re-reads the evidence, plus a
#              must_pass fixture AND at least one must_fail fixture, and
#              `scripts/ci/signoff.py prove` runs all of them. The gate is
#              demonstrated to say yes to one shape of evidence and no to
#              another.
#   RC-ONLY    the stage has no `check:`. Its verdict is the exit status of the
#              tool it invokes, and NOTHING in this repository demonstrates that
#              a bad result would produce a nonzero one. `prove` skips these
#              stages entirely — they do not appear in its case count, so the
#              green "N problem(s): 0" line says nothing whatever about them.
#
# A `report`-gate stage that is RC-ONLY costs a missing observation. A `block`
# stage that is RC-ONLY can pass a bad chip, which is why those are listed first
# and separately.
#
# THE LIMIT OF THE WORD "PROVEN". `prove` confirms a check is SELF-CONSISTENT
# WITH ITS FIXTURE. It does not confirm the fixture resembles the tool. A green
# prove run over an unrepresentative fixture is theatre, and this project has
# already shipped one: a hand-written 322-byte "aborted HAL run" with zero
# rule-tally lines, where a real 26 MB abort emits 40 — on the strength of which
# a load-bearing guard was documented as redundant. Fixture provenance is
# tracked in ci/fixtures/README.md and is the thing to distrust first.
#
# NOTHING EDITS THIS FILE AUTOMATICALLY. A coverage floor that the flow raises
# to whatever today's run produced is the exact defect this repository keeps
# finding in its own gates — an RV-via count set to the day's number, a DRC
# budget set to the day's number. Moving a row is the thirty seconds in which
# somebody reads the new case.
# ---------------------------------------------------------------------------


# === BLOCKING STAGES WITH NO PROOF THEY CAN FAIL ===========================
# NONE, as of 2026-08-18. Every `gate: block` stage now re-reads its evidence
# and is pinned by at least one planted fault it must reject.
#
# The last two closed were lec-selftest and lec-pnr, and they are worth naming
# because of what they were:
#
#   lec-selftest   itself a mutation harness for the LEC gate, and nothing
#                  checked that it ran its case list. A selftest that executes
#                  ZERO cases exits 0. Now asserts the exact count (4) AND
#                  re-derives the discrimination from the per-case transcripts,
#                  because a harness whose comparison has gone blind still
#                  prints "all 4 cases behaved as declared" if its expectation
#                  table drifted with it. That is fail-harness-blind.
#   lec-pnr        held a Conformal seat for up to six hours and reported on an
#                  exit status the Makefile recipe discards. A STALE
#                  logs/lec_pnr.log from an earlier run satisfied it and was
#                  contradicted by nothing — and this is the only comparison in
#                  the pipeline that can see what place/CTS/route did to the
#                  logic.
blocking-unproven       0

# === REPORT-ONLY STAGES WITH NO PROOF THEY CAN FAIL ========================
#   cdc                    gate: report by design (findings not yet triaged to zero).
#   lvs-preflight          gate: report by design (procurement blocker, expected red).
#   padring-power-domain   gate: report, RC-ONLY.
#   csr-category-ratchet   gate: report, RC-ONLY.
# All four are RC-ONLY. None can currently distinguish "the tool ran and found
# things" from "the tool did not run", which is the same failure both of cdc's
# HAL/Calibre siblings have already been caught making.
#
# 2 -> 4 on 2026-08-29. NOT a regression: padring-power-domain and
# csr-category-ratchet were already in the manifest and already RC-ONLY, and
# this ledger simply did not count them. That is the shape it exists to make
# impossible, and it was invisible because `must-fail-cases-total` and
# `stages-with-check` had drifted so far that the checker's output was ignored.
report-unproven         4

# === PROVEN GATES ==========================================================
# One row per stage with a `check:` + `check_proof:`, giving the number of
# must_fail fixtures. Asserted against ci/signoff.yaml by
# scripts/ci/check_mutation_coverage.py, red in BOTH directions: fewer than
# declared means a proof was dropped; more means one was added unreviewed.
#
# fixtures are under ci/fixtures/<stage>/. Every must_fail case below has been
# checked to trip exactly ONE clause of its guard unless noted -- a fixture that
# trips three guards at once pins none of them.
#
# ---------------------------------------------------------------------------
# WHOLESALE RE-REVIEW, 2026-08-29, and why the numbers move so far at once.
#
# This ledger last agreed with the manifest on 2026-08-18. Since then thirteen
# stages gained proofs and nobody moved a row, so
# `scripts/ci/check_mutation_coverage.py` had been printing sixteen problems --
# "ledger says 47, manifest has 114", eleven stages with no row at all -- on
# every run, to nobody. A checker whose output is always red is a checker that
# has stopped being read, which is the same end state as one that is always
# green.
#
# The rows below were re-derived from the manifest AND the fixture trees on
# 2026-08-29 and `signoff.py prove` was run over all of them: 173 cases, 0
# problems. Two things were fixed in the course of that, and both are the
# reason a re-review is not just a re-count:
#
#   * the sta-signoff fixtures still carried clock_count = 52 and ONE hold view,
#     from before the 2026-08-29 policy change to three hold views. The must_pass
#     case had been BROKEN ever since, and every must_fail case was passing on
#     CLOCK_COUNT / VIEW_ABSENT rather than on the defect it was cut to show.
#   * the build-freshness pass and fail-stale-exemption fixtures turned on the
#     (lec, gdsrun-20260821-slpads) exemption, which was withdrawn when `lec` was
#     repointed at @GRADED_BUILD@. They now turn on (gls-netlist, fp1505), which
#     is live.
#
# THREE ROWS ARE NEW STAGES, not new proofs of old ones: pins-reachable and
# vacuous-gates wire detectors this repository had and never called, and
# build-freshness had a proof and no row.
# ---------------------------------------------------------------------------
#
# stage              must_fail cases
chip-boundary           5
lint                    5
elab                    3
elab-strict             5
regress                 4
# NEW 2026-08-29. ASIC/asic-toolkit/ci/check-pins.sh existed and nothing ran it.
# Six cases: the defect of record (a submodule pin on no remote), --offline's
# UNVERIFIED non-answer, the zero-submodule vacuity case, an otherwise clean log
# with the pins.reachable row simply absent, a truncated log, and no log at all.
pins-reachable          6
# NEW 2026-08-29. The vacuous-gate scanner existed, ran only over the toolkit,
# and had never read one line of this chiplet. The load-bearing case is
# fail-chiplet-absent: a perfectly green scan of a corpus containing none of
# this repository.
vacuous-gates           5
# Had a full proof since 2026-08-24 and no row here.
build-freshness         7
rom-selftest            3
rom-content             6
rom-gds                 4
# 4 -> 6. fail-abort-points and fail-nonequivalent were added when the first
# real Conformal transcripts reached this repo.
lec                     6
lec-selftest            4
lec-pnr                 2
drc                     4
erc                     3
bnd                     4
logo                    2
ir-drop                 6
ir-drop-selftest        3
# 28, and that is not a typo. gls-netlist grades published gate-level simulation
# evidence per item, and each item contributes its own planted faults.
gls-netlist            28
padring-gds             4
# ONE case each, and both are thin. Named rather than rounded up: a single
# must_fail shows the gate is not stuck open and nothing more.
route-message-census    1
lvs-missing-connection  1
sta-signoff             5
sta-binding             5
evidence-report         4

# Total across the proven gates. `signoff.py prove` reports its own case count
# (must_pass + must_fail); this is the must_fail half, which is the half that
# demonstrates a gate can FAIL.
must-fail-cases-total  135

# Stages carrying a `check:` AND a complete proof (a must_pass plus at least one
# must_fail, every fixture present on disk). Cross-checked against the manifest
# so that adding a gate without a proof, or a proof without a gate, is visible
# here and not only in a transient prove run.
stages-with-check      27


# === PENDING -- OWNED BY ANOTHER SESSION, REPORTED BUT NOT GATED ===========
# EMPTY as of 2026-08-29. ir-drop and ir-drop-selftest landed and now carry rows
# of their own (6 and 3). The mechanism stays: a stage in flight belongs here
# rather than in the table above, because a row is a claim this ledger stands
# behind.
#
# THIS EXEMPTION IS NAMED, NOT GENERAL. Any OTHER stage that appears with a
# check and no row is a hard failure, because "a stage showed up and nobody
# looked at it" is how a decorative gate gets in.
pending-undeclared
