SYNTHETIC MARKER. Stands in for a Back-End package payload: the transistor-level
views whose absence this gap is about. No vendor content, and none is needed —
the probe reads only the name and type of the directory holding this file.
