# Real transcript excerpt (verdict text verbatim), fp1505 BEFORE the
# VDDIO/VSSIO route_special fix -- measured 2026-08-18, reproduced in
# docs/tapeout/51-erc-pg-labels.md section 5.2. Calibre's own PATHCHK checks
# are clean (VDD/VSS resolve), but the structural cross-check finds no
# top-cell text anywhere for VDDIO/VSSIO. run_erc.sh itself exits 0 here
# (RC_OK) -- this is exactly the case erc_census.py exists to catch, since a
# `check:` that trusted run_erc.sh's exit code would call this a pass.
== Calibre ERC (standalone, dummy source — no schematic compare) ==
  gds       : /home/dam1n19/SoCLabs/nanosoc-ethernet-chiplet/ASIC/eth-chiplet/build/fp1505/outputs/nanosoc_eth_chiplet_pads.gds
  top cell  : nanosoc_eth_chiplet_pads
  deck      : /tsmc65pdk/65/CMOS/LP/pdk/Calibre/lvs/calibre.lvs
  rundir    : /home/dam1n19/SoCLabs/nanosoc-ethernet-chiplet/ASIC/eth-chiplet/build/fp1505/work/erc_run
  power     : [VDD VDDIO]
  ground    : [VSS VSSIO]
  timeout   : 1800s

--- TOTAL RULECHECKS EXECUTED = 6
--- TOTAL RESULTS GENERATED = 0 (0)

== calibre exit status: 4 (informational — the compare is not read; only the ERC summary is) ==

== Calibre verdict (PATHCHK POWER/GROUND + the deck's own per-name check) ==
  OK — Calibre found at least one resolving power name and one resolving
  ground name, and none of [VDD VDDIO] / [VSS VSSIO] hit the deck's own
  no-data list (that list only covers the foundry's generic supply names —
  see the caveat above klayout_missing_names() for which names it can't see).

== Structural cross-check (klayout, independent of the deck's PATHCHK/
   POWER_NAME-GROUND_NAME universe): does EACH of [VDD VDDIO] / [VSS VSSIO]
   have text anywhere in the top cell? ==
  WARNING: NO top-cell text for: VDDIO VSSIO
           (present for the rest of [VDD VDDIO] / [VSS VSSIO].)
           A supply with text but no CONNECTED metal is still possible —
           only Calibre's PATHCHK (above) can see that; this only sees text.

== OVERALL: PARTIAL — Calibre's own checks are clean, but VDDIO VSSIO has/have no layout text anywhere. Report this before calling the design PG-clean. ==
