# Real transcript excerpt (verdict text verbatim), the reference GDS IMEC
# actually ran ERC on -- docs/tapeout/51-erc-pg-labels.md section 5.1. This
# is the calibration run that reproduces IMEC's own "No labels found in
# topcell" hard error exactly, both via Calibre's own ERC and an independent
# klayout structural scan. run_erc.sh's exit code IS non-zero here
# (RC_NOPG=4) -- included so erc_census.py's own no-OVERALL-line path is not
# the only way this census can fail; it must also correctly fail on a real,
# well-formed NOT SIGNED OFF transcript, not just an incomplete one.
== Calibre ERC (standalone, dummy source — no schematic compare) ==
  gds       : ASIC/genus-innovus/runs/20260811T103338Z_fill-verify/prev_outputs/nanosoc_eth_chiplet_pads_logo_full_L300.gds
  top cell  : nanosoc_eth_chiplet_pads
  deck      : /tsmc65pdk/65/CMOS/LP/pdk/Calibre/lvs/calibre.lvs
  rundir    : ASIC/genus-innovus/work/erc_run
  power     : [VDD VDDIO]
  ground    : [VSS VSSIO]
  timeout   : 1800s

--- WARNING: POWER, GROUND, LABELED or TEXT nets required by ERC operations do not exist.
--- TOTAL RULECHECKS EXECUTED = 6
--- TOTAL RESULTS GENERATED = 0 (0)

== calibre exit status: 4 (informational — the compare is not read; only the ERC summary is) ==

== Calibre verdict (PATHCHK POWER/GROUND + the deck's own per-name check) ==
  NOT MEANINGFUL — the layout carries NO power/ground text at all.
  This is the exact IMEC failure mode: "No labels found in topcell."
  Evidence, from this run's ERC summary / Calibre log:
      no POWER nets present
      no GROUND nets present

== Structural cross-check (klayout, independent of the deck's PATHCHK/
   POWER_NAME-GROUND_NAME universe): does EACH of [VDD VDDIO] / [VSS VSSIO]
   have text anywhere in the top cell? ==
  NO TEXT AT ALL for any declared name, anywhere in the top cell.

== OVERALL: NOT SIGNED OFF — at least one declared supply has no layout data at all. ==
  LIKELY CAUSE if this is a fresh stream: the GDS-out map used to produce it
  has `NAME <layer>/PIN` rows but no `NAME <layer>/SPNET` rows, so the
  special-route (power) grid went out unnamed. See
  ASIC/asic-toolkit/tech/tsmc65/derive.tcl (gdsmap_derive),
  docs/tapeout/48-imec-signoff-results-analysis.md §1.2 and
  docs/tapeout/51-erc-pg-labels.md.
